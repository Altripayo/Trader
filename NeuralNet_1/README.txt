////////// Structure of setup.txt file ///////////////////////////////////////

*Number of input nodes* (>= 1)

*Number of layers* (>= 1, last layer is output layer)
Each layer:
    *Number of nodes for layer* (>= 1)

*Number of input samples* (>= 1)
Each sample:
    Each input node:
        *Input*

//////////////////////////////////////////////////////////////////////////////